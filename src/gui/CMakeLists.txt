#############################################################################
# $Id: CMakeLists.txt 42471 2019-03-05 16:05:34Z ucko $
#############################################################################
##############################################################################
# CMakeLists.txt autogenerated from /export/home/dicuccio/cpp-cmake/gpipe-trunk/src/gui/Makefile.in
#

if (NCBI_EXPERIMENTAL_CFG)
NCBI_add_subdirectory(
  cache
  config
  core
  framework
  graph
  packages
  widgets
)
endif()

NCBI_add_subdirectory(
  utils
  opengl
  print
  objects
  objutils
  app
)

if(OFF)
#cmake_minimum_required(VERSION 2.8)
#project(CPP)

#set(top_src_dir     ${CMAKE_CURRENT_SOURCE_DIR}/..)
#set(abs_top_src_dir ${CMAKE_CURRENT_SOURCE_DIR}/..)
#set(build_root      ${CMAKE_BINARY_DIR})
#set(includedir      ${abs_top_src_dir}/include)

#set(EXECUTABLE_OUTPUT_PATH  ${build_root}/bin)
#set(LIBRARY_OUTPUT_PATH     ${build_root}/lib)
#set(CPP_INCLUDE_DIR         ${abs_top_src_dir}/include)

#include(build-system/cmake/CMakeChecks.cmake)
#include(build-system/cmake/CMakeMacros.cmake)
# Include projects from this directory

# Recurse subdirectories
add_subdirectory(utils)
add_subdirectory(opengl)
add_subdirectory(print)
add_subdirectory(objects)
add_subdirectory(objutils)
add_subdirectory(app)
endif()
